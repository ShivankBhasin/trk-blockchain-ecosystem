{
  "name": "trk-blockchain-contracts",
  "version": "1.0.0",
  "description": "TRK Blockchain Smart Contracts for BSC",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bscTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network bscMainnet",
    "verify": "hardhat verify --network bscMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "@openzeppelin/contracts": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
